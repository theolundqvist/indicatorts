/**
 * Exponential moving average (EMA).
 * @param period window period.
 * @param values values array.
 * @return EMA values.
 */
export declare function ema(period: number, values: number[]): number[];
